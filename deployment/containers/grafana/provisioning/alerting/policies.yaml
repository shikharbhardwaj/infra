apiVersion: 1

# Root notification policy: route everything to Discord. Group by alert name
# and host so a burst of related fires collapses into one message per host.
policies:
  - orgId: 1
    receiver: Discord
    group_by: ['alertname', 'host']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h

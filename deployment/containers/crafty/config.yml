# Crafty Controller Configuration
# This file is used by the installation script to set up the container.
# No podman secrets required - the admin account is created via the web UI
# on first access (see README.md).

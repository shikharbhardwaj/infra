---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: nextcloud-prod-daily-backup
  namespace: cloudnative-pg
spec:
  schedule: "0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: nextcloud
  immediate: true
  method: barmanObjectStore
